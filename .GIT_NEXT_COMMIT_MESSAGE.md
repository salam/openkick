fix(security): add auth middleware to all players/guardians routes

Add authMiddleware + requireRole("admin", "coach") to all 12 route
handlers in players.ts. The login endpoint remains public.
Update tests to include JWT auth headers.
